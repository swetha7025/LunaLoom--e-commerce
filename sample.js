 /* const crypto = require("crypto");
 const secret = crypto.randomBytes(64).toString("hex");
 console.log(secret);
  */
 //const crypto = "console.log(require('crypto').randomBytes(64).toString('hex'))"
 const crypto = require("crypto")
 const secret= crypto.randomBytes(64).toString("hex")
 console.log(secret);
 
